name: mvpa
channels:
  - conda-forge
  - defaults
dependencies:
  - ipykernel
  - ipython
  - jupyter
  - matplotlib
  - openssl
  - pip
  - python=3.11
  - setuptools
  - pip:
      - nilearn
      - seaborn
      - scikit-learn
      - statsmodels
      - statannotations
      - nibabel
      - mni-to-atlas